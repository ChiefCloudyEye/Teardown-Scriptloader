examplemodName = "Test Mod"	--Display name in the modloader UI.

examplemodDesc = "A simple test mod to show the basic setup of a mod in\n\"ChiefCloudyEye\'s Scriptloader\"."	--Display description in the modloader UI.

function examplemodInit()	--This function is called when the modloader recompiles due to enabling a new mod.
	
end

function examplemodTick()	--This function is called every tick.
	
end

function examplemodDraw()	--This function is called every UI tick while the UI is being rendered.
	
end

function examplemodUI()	--This function is called inside the mod's page of the options menu.
	
end